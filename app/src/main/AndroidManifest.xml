<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="janco.gatheringapp">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.CreateNewUserActivity" />
        <activity android:name=".Activities.NoticeOverviewActivity" />
        <activity android:name=".Activities.CreateNotice" />
        <activity android:name=".Activities.CreateNoticeSelectDateAndTime" />
        <activity android:name=".Activities.NoticeView" />
        <activity android:name=".Activities.UserConnectionActivity" />
        <activity android:name=".Activities.MessageOverviewActivity" />
        <activity android:name=".Activities.UserConnectionHistoryActivity"></activity>
    </application>

</manifest>
